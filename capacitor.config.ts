import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SistemaRotas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
